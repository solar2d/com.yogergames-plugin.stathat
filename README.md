Sources for the plugin `plugin.stathat`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.stathat" = {
            publisherId = "com.yogergames",
        },
    },
}
```
